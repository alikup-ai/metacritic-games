# TODO по этапам — Metacritic Games Service

**Обновлено:** 2026-09-07
**Статус:** Фаза 0, database foundation, checkpoint, **Phase 1A/1B/1C/1D** выполнены

Легенда: `[ ]` не начато · `[~]` в работе · `[x]` готово · 🔒 заблокировано решением

---

## Фаза 0 — Каркас

### Репозиторий и инструменты
- [x] `npm init`, TypeScript (strict), структура каталогов
- [x] ESLint + Prettier
- [x] Структура модулей по `ARCHITECTURE.md` §2
- [x] `dependency-cruiser`: запрет импорта `infrastructure` из `domain` (ADR-0001)
- [x] Схема конфигурации с валидацией при старте, `.env.example`

### Инфраструктура
- [x] `docker-compose.yml`: app + PostgreSQL с volume
- [ ] Dockerfile (multi-stage)
- [x] Инструмент миграций, автоприменение при старте
- [x] Пул подключений, helper для advisory lock
- [ ] Структурное логирование (JSON)
- [ ] `/healthz`, `/readyz`

### CI
- [ ] Lint → types → границы слоёв → unit → integration → сборка образа

**Критерий:** `docker compose up` поднимает сервис; нарушение границ слоёв роняет CI.

---

## Фаза 1 — Ядро ingestion

### 1.0 Исследование (до кода парсеров)
- [x] 🔬 **OQ-18:** исследование выполнено → `METACRITIC_PLATFORM_SCORES.md`
- [x] Оформить ADR по результату OQ-18 (→ ADR-0008)
- [x] Зафиксировать HTML-фикстуры: листинги, карточка игры, отзывы (9 файлов + README)
- [x] Фикстуры developer: форма со ссылкой, форма текстом, синтетическая пустая

### 1.1 Домен (без БД и сети)
- [x] `Game`, `GamePlatform` + типы (`catalog/domain/game.ts`)
- [x] `ProcessingDay` + типы фаз (ADR-0002)
- [x] `DailyClaim` + логика lease: `isLeaseExpired`, `shouldRunStage`, `nextStage` (ADR-0004)
- [x] `Run`, `RunEvent` (`monitoring/domain/run.ts`)
- [x] Порты: `GameRepository`, `ClaimRepository`, `ProcessingDayRepository`, `RunRepository`
- [x] Правило идентичности: UUID + `(source, source_slug)` (ADR-0003)
- [x] Правило developer/publisher: `assertDeveloperConsistency` (ADR-0003)
- [x] Юнит-тесты доменных правил (24 теста)
- [ ] Порт `GameCatalogSource` — вместе с парсерами в 1.2

### 1.2 Инфраструктура
- [x] Миграции: `game`, `game_platform`, `processing_day`, `daily_claim`, `run`, `run_event`
- [x] Индексы: metascore, release_date, trigram по названию, platform_slug
- [x] HTTP-клиент: timeout, retry (backoff+jitter), token bucket ≤1 req/s
- [x] Обработка 403 (abort, не ретраить), 429 (`Retry-After`), 5xx (ретрай)
- [ ] Circuit breaker по серии ошибок
- [x] Парсер листинга New Releases (`/game/`)
- [x] Парсер листинга See All (`/browse/.../new/?page=N`)
- [x] Парсер карточки: JSON-LD → fallback на DOM
- [x] Парсер developer: ОБЕ формы (ссылка + текст) **[ПРОВЕРЕНО]**
- [x] Ветка `developer = null` + `developer_status='unknown'` + ingestion log
- [x] Репозитории на PostgreSQL, upsert по `(source, source_slug)`
- [x] Golden-file тесты парсеров (69 тестов)

### 1.2a Phase 1A — выполнено 2026-09-07
- [x] Порт `GameCatalogSource` + нормализованные DTO
- [x] Типизированные ошибки `IngestionError` / `ParseError` по категориям
- [x] `MetacriticAdapter` — реализация порта
- [x] Структурное логирование без секретов и без полного HTML
- [x] Contract-тесты порта (16)
- [x] Live-тест `npm run test:metacritic-live` (вне CI, требует сеть)
- [x] Миграция 005: `score_scope = 'overall'` отдельно от `'overall_fallback'`

### 1.2b Phase 1B — application ingestion (выполнено 2026-09-07)
- [x] `IngestGameUseCase` — работает только с портами
- [x] Порт `UnitOfWork` + `PostgresUnitOfWork`: одна транзакция на игру
- [x] Идемпотентный upsert по `(source, source_slug)` с признаком создания (`xmax = 0`)
- [x] `GamePlatformRepository` выделен из `GameRepository`
- [x] Миграция 006: `is_active` / `last_seen_at` / `deactivated_at` (ADR-0011)
- [x] Мягкое отключение платформ вместо удаления
- [x] Пустой снимок не отключает платформы (защита от сбоя парсера)
- [x] `METACRITIC_FETCH_USERSCORE` + различение `fetched/absent/failed/disabled`
- [x] `COALESCE` защищает прежний Userscore от затирания
- [x] 8 событий ingestion + `IngestionEventSink`
- [x] 10 сценариев идемпотентности + конкурентность на реальной PostgreSQL
- [x] `docs/INGESTION.md`

### 1.3 Оркестрация
- [x] `FindNextClaimableGames`: New Releases → See All, добор до 20 claimed (ADR-0002)
- [x] Сохранение курсора `browse_page` сразу после чтения страницы
- [x] `claimBatch`: `INSERT ... ON CONFLICT DO NOTHING RETURNING`
- [x] Стадии с раздельным сохранением в `stages`
- [x] Reaper: возврат просроченных lease без создания дубля
- [x] Пул воркеров с ограничением параллелизма
- [x] Ежечасный планировщик под advisory lock
- [x] Единый путь запуска: `RunDailyProcessing`
- [x] `ProcessingDayProvider`: сутки по `PROCESSING_TIMEZONE`

### 1.4 Тесты корректности (обязательны)
- [x] Параллельный claim одного слага → успешен ровно один
- [x] Повторный запуск в тот же день → нет второго claim
- [x] Истёкший lease → игра возвращается в пул, `attempts++`
- [x] Перезапуск после части стадий → продолжение, не рестарт
- [x] Пересечение страниц листинга → нет пропусков (ADR-0002)
- [x] Смена суток → фаза сбрасывается на `new_releases`
- [x] Существующая игра обновляется, не дублируется

**Критерий:** 20 игр за запуск, корректный порядок источников, все тесты зелёные.

### Phase 1C — выполнено 2026-09-07
- [x] Миграция 007: `processing_day`, `source_strategy`, `last_page_hint`, `pages_scanned`
- [x] `ProcessingDayProvider` (абстракция часов) + `FixedProcessingDayProvider` для тестов
- [x] `FindNextClaimableGames` с защитой от зацикливания и дрейфа
- [x] `RunDailyProcessing` — оркестратор запуска
- [x] `runWorkerPool` + `withHeartbeat`
- [x] `DailyProcessingScheduler` (двухуровневая защита от наложения)
- [x] Порт `RunLock` + `PostgresRunLock`
- [x] 10 новых событий наблюдаемости
- [x] 22 обязательных сценария отказов на реальной PostgreSQL
- [x] `docs/WORKERS.md`

---

## Technical checkpoint (выполнен 2026-09-07)

Закрытие рисков, выявленных при реализации database foundation.

### Startup recovery для orphaned runs
- [x] Миграция 004: `heartbeat_at`, `owner_id`, `lock_key`, `recovered_at`, `recovery_reason`
- [x] Критерий orphaned run через `pg_locks` + heartbeat (ADR-0010), не «running старше N минут»
- [x] `isLockHeld` с корректной раскладкой 64-битного ключа (classid/objid)
- [x] `findOrphanedRuns`, `markRecovered`, `touchHeartbeat`, `listRunning`
- [x] Use case `recoverOrphanedRuns` + composition root `runStartupSequence`
- [x] Причина восстановления сохраняется в `run_events`
- [x] Тесты: обычный старт, осиротевший run, параллельный старт, повторное восстановление

### Golden fixtures
- [x] 9 фикстур: 4 страницы игр, 2 отзывов, 3 листингов
- [x] Документированы источник, дата, назначение (`tests/fixtures/metacritic/README.md`)
- [x] Объём сокращён с ~740 КБ до 9–36 КБ на файл
- [x] Тесты целостности фикстур, без сетевых запросов

### Документация политик
- [x] `RETENTION_POLICY.md` — периоды хранения (ADR не потребовался)
- [x] `RETRY_POLICY.md` — текущая модель повторов зафиксирована

---

## Production hardening (после MVP)

Не входит в объём тестового задания; вынесено осознанно.

- [ ] Планировщик очистки `run_events` (30 дней), пакетное удаление
- [ ] Очистка `runs` и `daily_claims` (90 дней)
- [ ] Партиционирование `run_events` по времени
- [ ] Метрики размера таблиц и глубины очереди
- [ ] Бэкап и восстановление БД
- [ ] Алертинг при росте доли `developer_status='unknown'` (признак смены разметки)

---

## Фаза 2 — Web UI

### API
- [ ] `GET /api/games` — пагинация, фильтр по платформам, поиск, сортировка
- [ ] `GET /api/games/:slug` — полная карточка
- [ ] `GET /api/platforms` — список платформ для фильтра
- [ ] Валидация параметров (zod), DTO вместо доменных объектов

### UI
- [ ] Страница списка, карточки с краткой информацией
- [ ] Страница игры: обложка, описание, трейлер
- [ ] Отображение developer; при `unknown` — «Разработчик не указан»
- [ ] Издатель — отдельным полем, без подмены developer
- [ ] Таблица оценок по платформам (с пометкой при `overall` / `overall_fallback`)
- [ ] Фильтр по платформам
- [ ] Поиск по названию с debounce
- [ ] Сортировка по рейтингу
- [ ] Состояние фильтров в URL
- [ ] Пустые состояния, ошибки, скелетоны
- [ ] Экранирование scraped-контента (запрет `dangerouslySetInnerHTML`)

**Критерий:** поиск, фильтр, сортировка и переход в карточку работают; p95 списка < 300 мс.

---

## Фаза 3 — Отзывы и LLM-резюме

- [x] Парсер отзывов критиков (JSON API, ADR-0012)
- [x] Парсер отзывов пользователей
- [x] Хранение `reviews` + `review_snapshots` с fingerprint и полнотой
- [x] Миграция 008: таблица reviews, платформа и completeness в снимках
- [x] Инкрементальная обработка: новый / изменённый / неизменный / исчезнувший
- [x] Пагинация с защитой от дубликатов и зацикливания
- [x] 6 событий наблюдаемости отзывов
- [x] 86 тестов (34 парсеры + 22 пагинация + 30 интеграционные)
- [ ] Порт `LlmProvider` 🔒 **OQ-6 — нужен провайдер и ключ**
- [ ] Адаптер LLM со структурированным выводом
- [ ] Промпт для критиков (likes / dislikes / verdict)
- [ ] Промпт для пользователей (отдельный)
- [ ] Ограничение объёма ввода с выборкой по всему диапазону оценок
- [ ] Grounding: запрет выдумывать факты вне отзывов
- [ ] **Fingerprint-gate:** пропуск вызова при неизменных отзывах
- [ ] Обработка «отзывов нет» без вызова LLM
- [ ] Логирование вызовов в `logs/ai/runtime/llm-calls.jsonl`
- [ ] Отображение двух резюме, визуально различимых
- [ ] Резюме — обогащающая стадия: сбой не ломает сохранение игры

**Критерий:** два раздельных резюме в карточке; повторный обход не тратит токены зря.

---

## Фаза 4 — Похожие игры

- [ ] Алгоритм схожести: жанры + платформы + developer/publisher + trigram
- [ ] Исключение самой игры, минимальный порог
- [ ] Таблица `similar_game`, пересчёт при пополнении БД
- [ ] `GET /api/games/:slug/similar`
- [ ] Блок в карточке, переход по клику
- [ ] Корректное поведение при малом объёме БД (меньше результатов, не мусор)

**Критерий:** обязательная часть ТЗ выполнена полностью.

---

## Фаза 5 — Мониторинг и Force Run

- [ ] Шина событий, сохранение в `run_event` до рассылки
- [ ] `GET /api/events` (SSE) с автопереподключением
- [ ] `GET /api/status`: текущий run, план дня, счётчики
- [ ] `POST /api/runs` 🔒 **OQ-8 — способ аутентификации**
- [ ] Rate limit на write-эндпоинты
- [ ] `202` при запуске / `409` при занятом lock
- [ ] Дашборд: статус run, воркеры, счётчики, фаза и курсор, лента событий, ошибка
- [ ] Кнопка Force Run с обработкой «уже выполняется»
- [ ] Воспроизведение недавней истории при подключении в середине run

**Критерий:** процесс виден в реальном времени; неаутентифицированный запуск отклоняется.

---

## Фаза 6 — YouTube (feature flag)

- [ ] Порт `VideoProvider`, адаптер YouTube Data API
- [ ] Поиск летсплеев, выбор самого популярного
- [ ] Каскад: официальные субтитры → авто → метаданные (ADR-0005)
- [ ] Сохранение `transcript_source`
- [ ] LLM-заключение по тексту
- [ ] Учёт квоты, кэш, мягкое отключение при исчерпании
- [ ] Отображение: заключение, ссылка, **явная пометка источника**
- [ ] Транскрипт как недоверенный ввод (экранирование)
- [ ] `FEATURE_YOUTUBE_ENABLED`, по умолчанию `false`
- [ ] Тест: выключенный флаг → сервис работает без ключей YouTube
- [ ] Тест: сбой YouTube не меняет статус игры

**Критерий:** заключение со ссылкой при включённом флаге; сбои не влияют на пайплайн.

---

## Фаза 7 — Деплой и сдача

- [ ] VPS, Docker Compose, reverse proxy + TLS
- [ ] Автозапуск, ротация логов, бэкап БД
- [ ] Наполнение БД перед демонстрацией
- [ ] Скрипт `logs:merge` — сборка merged audit trail (OQ-16)
- [ ] Скрипт `logs:scan` — проверка секретов (OQ-19)
- [ ] Проверка структуры `logs/ai/raw/<agent>/` и merged-лога
- [ ] README: описание, запуск, конфигурация, доступ к Force Run, ограничения
- [ ] Проверка сценариев восстановления на живом стенде
- [ ] Финальная проверка соответствия всем пунктам ТЗ

**Критерий:** живая ссылка, репозиторий с логами и документацией.

---

## Постоянные задачи (в течение всей разработки)

- [ ] Вести AI-логи по агентам без редактирования (OQ-16)
- [ ] Оформлять ADR на значимые решения
- [ ] Фиксировать новые допущения в документации
- [ ] Не изобретать селекторы: сначала исследование
- [ ] Держать `OPEN_QUESTIONS.md` в актуальном состоянии

---

## Заблокированные задачи

| Задача | Фаза | Блокирующий вопрос |
|---|---|---|
| Адаптер LLM | 3 | API-ключ на стенде (решение OQ-6 принято) |
| Деплой | 7 | Доступ к VPS и домену |

Все архитектурные блокеры сняты: OQ-6 → ADR-0006, OQ-8 → ADR-0007, OQ-18 → ADR-0008.
